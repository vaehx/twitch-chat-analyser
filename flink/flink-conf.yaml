web.port: 8081

# This will be replaced by the flink docker entrypoint script
jobmanager.rpc.address: localhost

# This will be replaced by the flink docker entrypoint script
taskmanager.numberOfTaskSlots: 1

taskmanager.heap.size: 512mb
taskmanager.network.memory.fraction: 0.03
taskmanager.network.memory.min: 32mb

jobmanager.heap.size: 256mb
