# Example bot configuration. Copy this to bot.properties and adjust as needed

# Get these from the Twitch Dev Console Applications page
twitch.client-id = ...
twitch.client-secret = ...

# Comma-separated
channels = xqcow, lirik

kafka.bootstrap-servers = kafka:9092
kafka.topic = TwitchMessages

db.jdbc-url = jdbc:postgresql://postgres:5432/twitch?user=postgres&password=password
#db.channels.table-name = channels

streams.table-name = streams
streams.update-cooldown.millis = 150000
